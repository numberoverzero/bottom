[tox]
envlist = py35, integ

[testenv]
deps =
    pytest
    flake8
    coverage
commands =
    coverage run --branch --source=bottom -m py.test --ignore=tests/integ
    coverage report -m
    flake8 bottom tests

[testenv:integ]
deps = pytest
commands = py.test --ignore=tests/unit
